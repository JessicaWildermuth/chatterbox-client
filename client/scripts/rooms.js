var Rooms = {
  //needs a method called ad that is a when used is an onclik event trigger
  //add the room to the drop options
  $roomButton: $('.roomButton'),

  add: function() {
    console.log('Room Button Works');
  }

  // store as an array? object with array? nested array? tuples?
};



// var newRoom = $('#newRoomName').val();
// var newRoomName = RoomsView.render({roomname: newRoom});
// $(newRoomName).appendTo(RoomsView.$select);